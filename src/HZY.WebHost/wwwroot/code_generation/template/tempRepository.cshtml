@using HZY.Infrastructure;
@using FreeSql.DatabaseModel;
@model HZY.Models.DTO.DevelopmentTool.GenContextDto
@{
    var className = Model.Name;
}
<pre>
using @(Model.Namespace).EFCore.DbContexts;
using @(Model.Namespace).Models.Entities;
using @(Model.Namespace).Repositories.BaseRepositories.Impl;

namespace @(Model.Namespace).Repositories
{
    /// <summary>
    /// 仓储 @(className)Repository
    /// </summary>
    public class @(className)Repository : AdminEfCoreBaseRepository<@className>
    {
        public @(className)Repository(AdminBaseDbContext context) : base(context)
        {
        }
    }
}
</pre>
